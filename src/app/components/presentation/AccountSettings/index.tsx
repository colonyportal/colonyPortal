import * as React from "react";

type Props = {
  
};

const AccountSettings: React.SFC<Props> = () => {
  return (
    <>
      <h1>Account Settings (Coming Soon)</h1>
      <p>The user can access their Colony Portal settings here.</p>
    </>
  );
};

export default AccountSettings;
