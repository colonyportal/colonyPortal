import * as React from "react";

type Props = {
  
};

const ColonyGenerator: React.SFC<Props> = () => {
  return (
    <>
      <h1>Colony Generator (Coming Soon)</h1>
      <p>Create a colony from scratch, or <b>generate one from a GitHub organization</b> or other pre-existing social graph.</p>
    </>
  );
};

export default ColonyGenerator;
